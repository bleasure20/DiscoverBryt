function setup() {
// Create a full screen canvas
createCanvas(windowWidth, windowHeight);
}
function draw() {
background(255,66,233);
//Place circle in the center of the screen
circle(mouseX,mouseY,50);
rect(mouseX+50,mouseY+50,40)
fill(mouseY,mouseX,255);





}
